project_name: gen-napi
before:
  hooks:
    # You may remove this if you don't use go modules.
    - go mod tidy
builds:
  - id: linux
    binary: gen-napi
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=1
      - CC=zig cc -v -target {{ if eq .Arch "arm64" }}aarch64{{else}}x86_64{{end}}-linux-gnu
      - CXX=zig c++ -v -target {{ if eq .Arch "arm64" }}aarch64{{else}}x86_64{{end}}-linux-gnu
    ldflags:
      - -s -w -X github.com/cryptodeal/gen-napi/cmd.version={{.Version}} -X github.com/cryptodeal/gen-napi/cmd.commit={{.Commit}} -X github.com/cryptodeal/gen-napi/cmd.commitDate={{.CommitDate}} -X github.com/cryptodeal/gen-napi/cmd.target={{.Env.GOOS}}
  - id: darwin
    binary: gen-napi
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    env:
      - CGO_ENABLED=1
      - CC=zig cc -v {{ if eq .Arch "arm64" }} -target aarch64-macos.11 --sysroot /Applications/Xcode_14.2.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/macosx13.1.sdk -I/usr/include -L/usr/lib -F/System/Library/Frameworks -framework Foundation{{end}}
      - CXX=zig c++ -v {{ if eq .Arch "arm64" }} -target aarch64-macos.11 --sysroot /Applications/Xcode_14.2.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/macosx13.1.sdk -I/usr/include -L/usr/lib -F/System/Library/Frameworks -framework Foundation{{end}}
    ldflags:
      - -s -w -X github.com/cryptodeal/gen-napi/cmd.version={{.Version}} -X github.com/cryptodeal/gen-napi/cmd.commit={{.Commit}} -X github.com/cryptodeal/gen-napi/cmd.commitDate={{.CommitDate}} -X github.com/cryptodeal/gen-napi/cmd.target={{.Env.GOOS}}
archives:
  - name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
